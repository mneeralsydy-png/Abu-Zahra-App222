name: Abu Zahra App Victory

on:
  push:
    branches: [ "main", "Push" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Build Project from Scratch
        run: |
          # 1. تنظيف شامل وإعادة بناء المجلدات
          rm -rf app build.gradle settings.gradle gradlew* gradle .gradle
          mkdir -p app/src/main/assets app/src/main/res/values app/src/main/java/com/abualzahra/parent

          # 2. ملف الإعدادات (صيغة مستقرة جداً)
          cat <<EOF > settings.gradle
          pluginManagement { repositories { google(); mavenCentral(); gradlePluginPortal() } }
          dependencyResolutionManagement {
              repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
              repositories { google(); mavenCentral() }
          }
          rootProject.name = "AbuAlZahra"
          include ':app'
          EOF

          # 3. ملفات البناء (إصدارات ثابتة لضمان النجاح)
          echo "plugins { id 'com.android.application' version '8.2.2' apply false; id 'org.jetbrains.kotlin.android' version '1.9.22' apply false }" > build.gradle
          
          cat <<EOF > app/build.gradle
          plugins { id 'com.android.application'; id 'org.jetbrains.kotlin.android' }
          android {
              namespace 'com.abualzahra.parent'
              compileSdk 34
              defaultConfig { 
                  applicationId "com.abualzahra.parent"
                  minSdk 24
                  targetSdk 34
                  versionCode 1
                  versionName "1.0"
              }
              compileOptions { sourceCompatibility JavaVersion.VERSION_17; targetCompatibility JavaVersion.VERSION_17 }
              kotlinOptions { jvmTarget = '17' }
          }
          dependencies { 
              implementation 'androidx.appcompat:appcompat:1.6.1'
              implementation 'com.google.android.material:material:1.11.0'
          }
          EOF

          # 4. الموارد والكود (HTML)
          echo '<!DOCTYPE html><html dir="rtl"><head><meta charset="utf-8"></head><body style="background:#eeeeee; text-align:center;"><h1>تطبيق أبو الزهراء</h1><p>يعمل بنجاح الآن!</p></body></html>' > app/src/main/assets/index.html
          echo '<?xml version="1.0" encoding="utf-8"?><resources><string name="app_name">AbuAlZahra</string></resources>' > app/src/main/res/values/strings.xml
          echo '<?xml version="1.0" encoding="utf-8"?><resources><style name="Theme.AbuAlZahra" parent="Theme.MaterialComponents.DayNight.NoActionBar"/></resources>' > app/src/main/res/values/styles.xml
          
          cat <<EOF > app/src/main/AndroidManifest.xml
          <?xml version="1.0" encoding="utf-8"?>
          <manifest xmlns:android="http://schemas.android.com/apk/res/android">
              <uses-permission android:name="android.permission.INTERNET" />
              <application android:label="@string/app_name" android:theme="@style/Theme.AbuAlZahra">
                  <activity android:name=".MainActivity" android:exported="true">
                      <intent-filter><action android:name="android.intent.action.MAIN" /><category android:name="android.intent.category.LAUNCHER" /></intent-filter>
                  </activity>
              </application>
          </manifest>
          EOF

          cat <<EOF > app/src/main/java/com/abualzahra/parent/MainActivity.kt
          package com.abualzahra.parent
          import android.os.Bundle
          import android.webkit.WebView
          import androidx.appcompat.app.AppCompatActivity
          class MainActivity : AppCompatActivity() {
              override fun onCreate(savedInstanceState: Bundle?) {
                  super.onCreate(savedInstanceState)
                  val web = WebView(this)
                  web.settings.javaScriptEnabled = true
                  web.loadUrl("file:///android_asset/index.html")
                  setContentView(web)
              }
          }
          EOF

          # 5. تثبيت نسخة Gradle المستقرة يدوياً (الحل السحري لخطأ HasConvention)
          gradle wrapper --gradle-version 8.2.2

      - name: Build APK
        run: |
          chmod +x gradlew
          ./gradlew :app:assembleDebug --no-daemon

      - name: Upload Finished APK
        uses: actions/upload-artifact@v4
        with:
          name: AbuAlZahra-Final-App
          path: app/build/outputs/apk/debug/*.apk
