name: Abu Zahra Final Victory Build

on:
  push:
    branches: [ "main", "Push" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Reconstruction & Fix Environment
        run: |
          # 1. تنظيف شامل لضمان عدم وجود ملفات تالفة
          rm -rf app build.gradle settings.gradle gradlew gradlew.bat gradle .gradle
          
          # 2. إنشاء المجلدات الأساسية (Assets و Java)
          mkdir -p app/src/main/assets
          mkdir -p app/src/main/res/values
          mkdir -p app/src/main/java/com/abualzahra/parent

          # 3. إعدادات Gradle المستقرة (حل مشكلة Gradle 9)
          cat <<EOF > settings.gradle
          pluginManagement { repositories { google(); mavenCentral(); gradlePluginPortal() } }
          dependencyResolutionManagement {
              repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
              repositories { google(); mavenCentral() }
          }
          rootProject.name = "AbuAlZahra"
          include ':app'
          EOF

          # 4. ملفات البناء (Root & App)
          echo "plugins { id 'com.android.application' version '8.2.0' apply false; id 'org.jetbrains.kotlin.android' version '1.9.0' apply false }" > build.gradle
          
          cat <<EOF > app/build.gradle
          plugins { id 'com.android.application'; id 'org.jetbrains.kotlin.android' }
          android {
              namespace 'com.abualzahra.parent'
              compileSdk 34
              defaultConfig { 
                  applicationId "com.abualzahra.parent"
                  minSdk 24
                  targetSdk 34
                  versionCode 1
                  versionName "1.0"
              }
              compileOptions { sourceCompatibility JavaVersion.VERSION_17; targetCompatibility JavaVersion.VERSION_17 }
          }
          dependencies { 
              implementation 'androidx.appcompat:appcompat:1.6.1'
              implementation 'com.google.android.material:material:1.11.0'
          }
          EOF

          # 5. وضع ملف الويب (المسار الذي حددته)
          echo '<!DOCTYPE html><html dir="rtl"><body><h1 style="text-align:center;">تم بناء تطبيق أبو الزهراء بنجاح!</h1></body></html>' > app/src/main/assets/index.html

          # 6. الموارد والمانيفست
          echo '<?xml version="1.0" encoding="utf-8"?><resources><string name="app_name">AbuAlZahra</string></resources>' > app/src/main/res/values/strings.xml
          echo '<?xml version="1.0" encoding="utf-8"?><resources><style name="Theme.AbuAlZahra" parent="Theme.MaterialComponents.DayNight.NoActionBar"/></resources>' > app/src/main/res/values/styles.xml
          
          cat <<EOF > app/src/main/AndroidManifest.xml
          <?xml version="1.0" encoding="utf-8"?>
          <manifest xmlns:android="http://schemas.android.com/apk/res/android">
              <uses-permission android:name="android.permission.INTERNET" />
              <application android:label="@string/app_name" android:theme="@style/Theme.AbuAlZahra">
                  <activity android:name=".MainActivity" android:exported="true">
                      <intent-filter><action android:name="android.intent.action.MAIN" /><category android:name="android.intent.category.LAUNCHER" /></intent-filter>
                  </activity>
              </application>
          </manifest>
          EOF

          # 7. كود MainActivity لفتح ملف Assets
          cat <<EOF > app/src/main/java/com/abualzahra/parent/MainActivity.kt
          package com.abualzahra.parent
          import android.os.Bundle
          import android.webkit.WebView
          import androidx.appcompat.app.AppCompatActivity
          class MainActivity : AppCompatActivity() {
              override fun onCreate(savedInstanceState: Bundle?) {
                  super.onCreate(savedInstanceState)
                  val web = WebView(this)
                  web.settings.javaScriptEnabled = true
                  web.loadUrl("file:///android_asset/index.html")
                  setContentView(web)
              }
          }
          EOF

          # 8. توليد الجرادل المستقر
          gradle wrapper --gradle-version 8.2

      - name: Build APK
        run: |
          chmod +x gradlew
          ./gradlew :app:assembleDebug --no-daemon

      - name: Upload Final APK
        uses: actions/upload-artifact@v4 # الحل النهائي لمشكلة الصورة 21 (استخدام الإصدار الرابع)
        with:
          name: AbuAlZahra-Success-APK
          path: "**/*.apk" # حل مشكلة المسار في الصورة 22
